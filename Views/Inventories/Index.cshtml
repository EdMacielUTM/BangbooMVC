@model IEnumerable<BangbooMVC.Models.Inventory>

@{
    ViewBag.Title = "Index";
}

<h2>Inventory</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastRestockDate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Bangboo.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Quantity)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastRestockDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Bangboo.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Inventory }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Inventory }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Inventory })
        </td>
    </tr>
}

</table>
